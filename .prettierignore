dist/
node_modules/
playwright-report/
test-results/
coverage/

# npm owns this file's formatting; reformatting it churns the diff on every install.
package-lock.json
*.woff2
*.woff

# Markdown is authored by hand (governing docs are Aditya-owned; tables/prose
# shouldn't be auto-reflowed). Prettier formats code only.
**/*.md
